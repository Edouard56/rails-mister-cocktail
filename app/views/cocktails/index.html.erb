

<h1 class="text-center">List of Cocktails</h1>

<div class="container">
  <div class="row">
    <% @cocktails.each do |c| %>
      <div class="col-xs-12 col-sm-4">
        <%= render 'cocktail_card', cocktail: c %>
      </div>
    <% end %>
  </div>
</div>

  <div class="text_center padded">
<%= link_to 'Create a new cocktail', new_cocktail_path %>
  </div>
